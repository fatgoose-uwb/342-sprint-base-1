class Sorter {
public:
    static void bubble_sort(int *data, int length);
    /*
     * OK to add more function signature here. The implementation of the new function should be
     * added to the problem_2.cpp file.
     */
};